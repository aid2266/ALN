#include <iostudio>

using namespace std; 

int main(){
    // PARAMETRES: la matriu resolta LU, vector b, vector perm
    // tenim Ax = b, tenim llavors PA = LU, PAx = LUx
    // resolem LUx = Pb -- SUPER IMPORTANT la matriu P
    // retorna solucio x del sistema
    // resol (a(matriu LU), b, perm, n (size de la matriu), x* (per ref))
    
    // calcul del residuu (per no fer copia de la matriu, podem calc error en main.cc )
    
    
}
