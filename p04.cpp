#include <iostream>
#include <iomanip>
#include <cmath>

using namespace std; 
// ibp dos veces
// J(k) = 1/pi - k(k-1)/pi^2 * J(k-2)

int main(){
    // la idea: quieres hacer iteraciones de k
    // haces un for loop que te devuelve una 
    
    int k; // input valor de k 
    float pi = .. // set value of pi
    
    for (float i = 2; i <= k ; i++){
        // 
        double cp =  // hacemos una copia del anterior
        
    }
    
}
